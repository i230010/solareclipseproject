[project]
name = "solareclipseproject"
version = "1.0.0"
description = "Solar Eclipse Programming in Python"
readme = "README.md"
requires-python = ">=3.14"
authors = [{ name = "Irsyad (i230010)" }]
license = { file = "LICENSE" }

classifiers = [
    "Programming Language :: Python :: 3",
]

dependencies = [
    "skyfield",
    "numpy",
    "vector",
]
